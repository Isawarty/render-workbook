# engine/rhi

这个目录从 **P2** 开始才有内容。

P1 的全部 Vulkan 代码写在 `projects/p01-triangle/src/` 下。做完 P1-t09 之后，
你写过的 instance / device / swapchain / 同步这几块会被提炼到这里，成为后续所有项目复用的基础。

换句话说：**这个目录里将来的每一行，都是你在 P1 里亲手写过一遍的。**
