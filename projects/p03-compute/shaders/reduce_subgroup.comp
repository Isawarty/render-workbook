#version 450
#extension GL_KHR_shader_subgroup_basic : require
#extension GL_KHR_shader_subgroup_arithmetic : require
// TODO(p03-t02): subgroupAdd + 每个 subgroup 局部和的二级汇总。
layout(local_size_x = 256) in;
layout(std430, set = 0, binding = 0) readonly buffer InBuf { float inData[]; };
layout(std430, set = 0, binding = 1) writeonly buffer OutBuf { float outData[]; };
layout(push_constant) uniform Push { uint n; } pc;
void main() {
    if (gl_LocalInvocationID.x == 0u) outData[gl_WorkGroupID.x] = 0.0;
}
