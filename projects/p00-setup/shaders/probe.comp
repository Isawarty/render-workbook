#version 450

// P0 的存在只为验证构建期 GLSL -> SPIR-V 这条链路是通的。
// 内容本身无关紧要，它不会被真正 dispatch。
layout(local_size_x = 64) in;

layout(std430, binding = 0) buffer Data {
    float values[];
} data;

void main() {
    uint i = gl_GlobalInvocationID.x;
    data.values[i] = data.values[i] * 2.0;
}
